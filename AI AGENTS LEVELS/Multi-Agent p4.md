Part 4 — Building and Testing the System
Slides: Project structure for an orchestrator + specialists → LLM routing vs if/else routing in code → Logging orchestrator decisions for traceability → Testing each specialist in isolation before connecting → Common failure modes: wrong routing, lost context, runaway loops → CTA: pick 2–3 pipelines you already have and build a router for them
