Level 3 — Orchestrator + Specialists: The Multi-Agent System
Part 1 — The Orchestrator Mental Model
Slides: Why pipelines break when you have too many of them → The traffic controller analogy → What an orchestrator does (intent parsing → agent selection → result handling) → Rule-based router vs LLM router: the difference → When you need an orchestrator vs when a pipeline is enough